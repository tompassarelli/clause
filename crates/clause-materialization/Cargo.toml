[package]
name = "clause-materialization"
version = "0.0.0"
edition = "2024"
license = "MIT OR Apache-2.0"
readme = "README.md"
rust-version = "1.96"
publish = false

# This crate is intentionally manifest-local until the shared-workspace
# integrator admits the package split.
[workspace]

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "warn"
pedantic = "warn"

[dev-dependencies]
serde_json = "1"
