Bool
Item

selected:
  ?item shape Item
  ?selected shape Bool
  ?item:
    selected: ?selected

mode selected given item yields selected: one

first
  member of: Item
first selected false

on toggle ?item
  when
    ?item selected ?prior
  withdraw
    ?item selected ?prior
  include
    ?item selected (?prior = false)
